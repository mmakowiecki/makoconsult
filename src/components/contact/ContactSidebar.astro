---
import type { Lang } from '../../i18n/ui';
import Icon from '../ui/Icon.astro';

interface Props {
  lang: Lang;
}

const { lang } = Astro.props;

const content = {
  en: {
    basedIn: 'Based in',
    location: 'Hamburg, Germany',
    workingWith: 'Working with',
    audience: 'B2B companies in tech, FMCG, industrial, and professional services — typically 25-250 employees navigating complexity in positioning, growth, or modernization — plus agencies looking for a strategic partner.',
    responseTime: 'Response time',
    responseValue: 'Within 24 business hours',
    linkedin: 'LinkedIn Profile',
  },
  de: {
    basedIn: 'Standort',
    location: 'Hamburg, Deutschland',
    workingWith: 'Zusammenarbeit mit',
    audience: 'Tech-Scale-ups, FMCG- und Konsumgüterunternehmen, industrielle KMUs, Professional-Service-Unternehmen, Consumer- und E-Commerce-Marken sowie Agenturen, die einen strategischen Partner suchen.',
    responseTime: 'Antwortzeit',
    responseValue: 'Innerhalb von 24 Geschäftsstunden',
    linkedin: 'LinkedIn-Profil',
  },
};

const c = content[lang];
---

<div class="bg-warm-white rounded-sm p-8">
  <h4 class="font-heading text-lg font-bold text-graphite mb-1">Michael Makowiecki</h4>
  <p class="text-sm text-steel mb-1">Strategic Marketing Consultant</p>
  <p class="text-sm text-steel mb-6">Founder, MAKO Consult</p>

  <div class="space-y-5">
    <div>
      <p class="text-xs font-semibold uppercase tracking-widest text-ember mb-1">{c.basedIn}</p>
      <p class="text-steel">{c.location}</p>
    </div>
    <div>
      <p class="text-xs font-semibold uppercase tracking-widest text-ember mb-1">{c.workingWith}</p>
      <p class="text-steel">{c.audience}</p>
    </div>
    <div>
      <p class="text-xs font-semibold uppercase tracking-widest text-ember mb-1">{c.responseTime}</p>
      <p class="text-steel">{c.responseValue}</p>
    </div>
    <div class="pt-2">
      <p class="text-xs font-semibold uppercase tracking-widest text-ember mb-3">{lang === 'en' ? 'Connect with me' : 'Vernetzen Sie sich mit mir'}</p>
      <a
        href="https://www.linkedin.com/in/mmakowiecki/"
        target="_blank"
        rel="noopener noreferrer"
        class="block hover:opacity-80 transition-opacity"
        aria-label="LinkedIn"
      >
        <Icon name="linkedin-color" class="w-3/4 [&>svg]:w-full [&>svg]:h-auto" />
      </a>
    </div>
  </div>
</div>
